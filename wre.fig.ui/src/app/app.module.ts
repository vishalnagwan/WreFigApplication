// AppModule is no longer used — the app bootstraps via bootstrapApplication()
// in main.ts with app.config.ts. This file is kept only to avoid build errors
// from any residual references; it is safe to delete it entirely.
import { NgModule } from '@angular/core';

@NgModule({})
export class AppModule {}
